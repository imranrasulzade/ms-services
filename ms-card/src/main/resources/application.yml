spring:
  application:
    name: ms-card
  devtools:
    remote:
      restart:
        enabled: true

server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka